import type { Messages } from "./en";

/**
 * Not translated yet. Every key falls back to English on its own, so a partial
 * catalogue is a perfectly good contribution — add the lines you know and
 * leave the rest out.
 */
const cs: Partial<Messages> = {};

export default cs;
